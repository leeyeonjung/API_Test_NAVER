# ============================================
# 사용 방법 요약
# ============================================
# 1. NAVER_CLIENT_ID, NAVER_CLIENT_SECRET, NAVER_REDIRECT_URI 값을 입력합니다.
# 2. 명령어 실행: python src/utils/make_url.py → 인증 URL 생성
# 3. 생성된 URL로 이동 후 로그인/인증 → code 값 복사
# 4. 명령어 실행: python src/utils/code_to_token.py → 토큰 발급
# 5. 명령어 실행: pytest testcase/test_api.py → API 테스트 실행
# ============================================

# 클라이언트 ID (애플리케이션 등록 시 발급받은 Client ID)
NAVER_CLIENT_ID=your_client_id_here
# 예시: NAVER_CLIENT_ID=abc123def456ghi789

# 클라이언트 시크릿 (Secret Key)
NAVER_CLIENT_SECRET=your_client_secret_here
# 예시: NAVER_CLIENT_SECRET=xyz789uvw456rst123

# 리디렉트 URI (Callback URL)
# 네이버 개발자센터에 등록한 Redirect URI와 동일해야 합니다.
NAVER_REDIRECT_URI=your_redirect_uri_here
# 예시: NAVER_REDIRECT_URI=http://localhost:8080/callback
# 예시: NAVER_REDIRECT_URI=https://yourdomain.com/callback

# ============================================
# 인증 요청 URL (자동 생성)
# ============================================
# 아래 항목은 다음 명령어 실행 후 자동으로 채워집니다:
# python src/utils/make_url.py
AUTHORIZE_URL=

# ============================================
# 인증 코드 및 토큰
# ============================================
# 아래 값들은 인증 과정 중 자동으로 채워집니다.

# 인증 코드 (Authorization Code)
# 브라우저에서 AUTHORIZE_URL에 접속 후, 인증 완료 시 전달되는 code 값을 입력하세요.
CODE=

# 액세스 토큰 (Access Token)
# 아래 명령어 실행 후 자동으로 채워집니다:
# python src/utils/code_to_token.py
ACCESS_TOKEN=

# 리프레시 토큰 (Refresh Token)
# 위와 동일한 명령어 실행 후 자동으로 채워집니다.
# 액세스 토큰이 만료되면 자동으로 재발급됩니다.
REFRESH_TOKEN=